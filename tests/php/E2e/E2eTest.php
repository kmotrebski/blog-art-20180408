<?php declare(strict_types=1);

/*
 * Imagine that whole test suite of end-2-end or UI tests is located here. This
 * can be e.g. Selenium tests based on Facebook Webdriver, so it will have some
 * e.g. Docker container configuration, phpunit.xml configuration,
 * docker-compose file etc.
 */
